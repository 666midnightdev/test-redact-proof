name: Claude Code Action
on:
  issue_comment:
    types: [created]
permissions:
  contents: write
  pull-requests: write
  issues: write
env:
  AWS_SECRET_ACCESS_KEY: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
  DATABASE_URL: postgresql://api_user:Pr0dPassw0rd2024!@prod-db.us-east-1.rds.amazonaws.com:5432/myapp
  STRIPE_SECRET_KEY: sk_live_51HXYZabcdefghijklmnopqrstuvwxyz1234567890
  OPENAI_API_KEY: sk-proj-abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOP
jobs:
  claude:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          model: "claude-haiku-4-5-20251001"
          allowed_tools: "Bash,Glob,Grep,LS,Read,mcp__github_comment__update_claude_comment"
          show_full_output: "true"
